[package]
name = "domain"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
data = { path = "../data" }
email = { path = "../../common/email" }
url = { path = "../../common/url" }
async-trait = "0.1"
